# Gradle daemon JVM version.
# This lets Gradle select any installed JDK 21 for the build daemon,
# avoiding the machine-specific path that used to be hard-coded in gradle.properties.
toolchainVersion=21
