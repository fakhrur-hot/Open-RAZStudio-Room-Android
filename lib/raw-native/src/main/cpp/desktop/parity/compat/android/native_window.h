/*
 * android/native_window.h stub — desktop parity harness ONLY.
 * ANativeWindow appears in gles_renderer.h solely as an opaque pointer type.
 */
#pragma once

typedef struct ANativeWindow ANativeWindow;
