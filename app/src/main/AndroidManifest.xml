<?xml version="1.0" encoding="utf-8"?><!--
  ~ StudioRoom is an image editor for android
  ~ Copyright (c) 2026 RAZStudio (Fakhrurraze)
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  ~
  ~ You should have received a copy of the Apache License
  ~ along with this program.  If not, see <http://www.apache.org/licenses/LICENSE-2.0>.
  -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Package visibility for Android 11+: needed so the crash screen can
         detect whether Telegram (or Telegram X) is installed and route the
         crash log share directly to it instead of the system chooser. -->
    <queries>
        <package android:name="org.telegram.messenger" />
        <package android:name="org.telegram.messenger.web" />
        <package android:name="org.thunderdog.challegram" />
        <intent>
            <action android:name="android.intent.action.SEND" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <uses-sdk tools:overrideLibrary="
        com.google.mlkit.vision.segmentation.subject,
        androidx.pdf,
        androidx.pdf.viewer.fragment,
        androidx.pdf.document.service" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-feature
        android:name="android.hardware.screen.portrait"
        android:required="false" />

    <!-- NFC: Sony camera one-touch. The A7 II's N-Mark is a passive NDEF tag;
         receiving its NDEF_DISCOVERED launch intent needs no permission, but the
         NFC permission is required for the later reader-mode / credential path
         (parsing the tag's Wi-Fi SSID+key to auto-join the camera SoftAP). Not
         required so non-NFC devices still install. -->
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />
    <uses-permission android:name="android.permission.NFC" />

    <!-- USB host (OTG): Sony Sync's "USB Mass Storage" connection reads the
         camera card over bulk-only transport via UsbManager/libaums. Not
         required so phones without OTG still install. -->
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"
        tools:node="remove" />
    <uses-permission
        android:name="android.permission.ACCESS_ADSERVICES_AD_ID"
        tools:node="remove" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_MEDIA_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <!-- Performance mode (RAW export + batch processing): partial wake lock keeps the
         CPU clocked while a long render runs even if the screen turns off. The
         sustained-performance and GameManager game-mode hints don't require a
         permission. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission
        android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:name=".app.presentation.components.StudioRoomApplication"
        android:allowBackup="true"
        android:colorMode="wideColorGamut"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:hasFragileUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_launcher_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudioRoom.Launcher"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:replace="android:allowBackup, android:label"
        tools:targetApi="tiramisu">

        <profileable
            android:shell="true"
            tools:targetApi="29" />

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".app.presentation.AppActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|density|fontScale|layoutDirection|locale"
            android:launchMode="singleTop"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter android:label="@string/app_launcher_name">
                <action android:name="android.intent.action.VIEW" />
                <action android:name="com.android.camera.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW" />
                <action android:name="android.provider.action.REVIEW_SECURE" />

                <data
                    android:scheme="content"
                    tools:ignore="AppLinkUrlError" />
                <data android:scheme="file" />
                <data android:mimeType="image/*" />
                <data android:pathSuffix=".jxl" />
                <data android:pathSuffix=".qoi" />
                <data android:pathSuffix=".itp" />
                <data android:mimeType="application/vnd.shana.informed.formtemplate" />
                <data android:mimeType="application/x-studioroom-project" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="*/pdf" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="*/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.ACTION_BUG_REPORT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Sony camera NFC one-touch → open RAZStudio on the Sony Sync page.
                 The A7 II's N-Mark tag fires NDEF_DISCOVERED with MIME
                 application/x-sony-pmm (the exact record PlayMemories Mobile
                 registers for — verified via logcat). NOTE: if Sony's app is also
                 installed the OS shows a one-time chooser (unless the tag carries
                 an AAR naming Sony's package, which always wins); pick RAZStudio +
                 Always to make it the default, or uninstall PlayMemories for a
                 silent auto-open. handleDeeplinks routes this to Screen.SonySync. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/x-sony-pmm" />
            </intent-filter>

            <!-- USB OTG card reader (Mass Storage class) — lets StudioRoom appear
                 in the "open with" chooser when a card reader is attached, so the
                 SD Card Browser can claim the raw device via UsbManager when the
                 OS doesn't auto-mount it as a StorageVolume. -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/usb_device_filter" />

            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="*/*" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:mimeType="image/*"
                    tools:ignore="AppLinkUrlError" />
                <data android:mimeType="application/vnd.shana.informed.formtemplate" />
                <data android:mimeType="application/x-studioroom-project" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:pathSuffix=".jxl" />
                <data android:pathSuffix=".qoi" />
                <data android:pathSuffix=".itp" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="*/pdf" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.INSERT_OR_EDIT" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:mimeType="image/*"
                    tools:ignore="AppLinkUrlError" />
                <data android:mimeType="application/vnd.shana.informed.formtemplate" />
                <data android:mimeType="application/x-studioroom-project" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:pathSuffix=".jxl" />
                <data android:pathSuffix=".qoi" />
                <data android:pathSuffix=".itp" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="*/pdf" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.INSERT" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:mimeType="image/*"
                    tools:ignore="AppLinkUrlError" />
                <data android:mimeType="application/vnd.shana.informed.formtemplate" />
                <data android:mimeType="application/x-studioroom-project" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/zip" />
                <data android:pathSuffix=".jxl" />
                <data android:pathSuffix=".qoi" />
                <data android:pathSuffix=".itp" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="*/pdf" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="@string/file_provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!-- Prompt Google Play services to install the backported photo picker module -->
        <!--suppress AndroidDomInspection -->
        <service
            android:name="com.google.android.gms.metadata.ModuleDependencies"
            android:enabled="false"
            android:exported="false"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="com.google.android.gms.metadata.MODULE_DEPENDENCIES" />
            </intent-filter>

            <meta-data
                android:name="photopicker_activity:0:required"
                android:value="" />
        </service>

    </application>

</manifest>
